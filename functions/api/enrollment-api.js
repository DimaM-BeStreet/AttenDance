const {onCall} = require('firebase-functions/v2/https');

// Placeholder for enrollment-related Cloud Functions
exports.placeholder = onCall(async (request) => {
  return { message: 'Enrollment API placeholder' };
});
