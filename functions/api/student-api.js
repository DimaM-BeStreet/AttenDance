const {onCall} = require('firebase-functions/v2/https');

// Placeholder for student-related Cloud Functions
exports.placeholder = onCall(async (request) => {
  return { message: 'Student API placeholder' };
});
