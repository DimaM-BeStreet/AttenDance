const {onCall} = require('firebase-functions/v2/https');

// Placeholder for attendance-related Cloud Functions
exports.placeholder = onCall(async (request) => {
  return { message: 'Attendance API placeholder' };
});
